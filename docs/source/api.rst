.. _api:

API
===

trlX uses a single entrypoint for training with `trlx.train` which will dispatch training given the training config and the necessary arguments for a specific training routine.

Training
--------

.. autofunction:: trlx.train

