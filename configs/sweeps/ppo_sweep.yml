tune_config:
  mode: "max"
  metric: "reward/mean"
  search_alg: "random"
  scheduler: "fifo"
  num_samples: 16

# https://docs.ray.io/en/latest/tune/api_docs/search_space.html#tune-sample-docs
optimizer.kwargs.lr:
  strategy: "loguniform"
  values: [0.000001, 0.0001]
method.init_kl_coef:
  strategy: "uniform"
  values: [0, 0.2]
method.vf_coef:
  strategy: "uniform"
  values: [0.25, 1.5]
